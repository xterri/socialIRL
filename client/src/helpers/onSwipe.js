
export const EventLeftSwipe = (item) => { 
    // archive/save event id to user's db

    // remove/filter event from the state

    // return new updated list (done in the context?)
    console.log(item);
};

export const EventRightSwipe = (item) => { 
    // remove/filter event from the state

    // return new updated list (done in the context?)
    console.log('RIGHT / DISLIKE SWIPE', item.title);
};
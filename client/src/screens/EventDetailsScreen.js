import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

const EventDetailsScreen = () => {
    return (
        <View>
            <Text style={{ fontSize: 40 }}>EVENT DETAILS</Text>
        </View>
    );
};

const styles = StyleSheet.create({});

export default EventDetailsScreen;